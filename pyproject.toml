[project]
name = "snowpitch"
version = "1.1.0"
requires-python = ">=3.9"

[tool.ruff]
line-length = 100
target-version = "py39"
extend-exclude = ["dbt/target", "apps/web", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501", "UP017", "UP038", "B905"]
