# Optional Blender / CC0 .glb models
#
# Drop files here to upgrade the 3D scene:
#   ball.glb
#   player.glb
#   stadium.glb
#
# If a file is missing, MatchGame3D falls back to primitive geometry
# (capsule players, sphere ball, procedural pitch). Snowflake telemetry
# is unchanged either way.

Place your models in this folder when ready.
