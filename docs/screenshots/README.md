# Screenshots

Add these before publishing the portfolio:

1. `demo.gif` — 60s: kickoff → chance → goal replay → pack → LiveOps alerts
2. `match.png` — night Whiteout pitch with snow + floodlights
3. `ops.png` — Recharts board showing three alerts + statistical tests

On macOS: QuickTime screen recording, then `ffmpeg -i rec.mov -vf "fps=12,scale=960:-1" docs/screenshots/demo.gif`
